// Circumference of a Circle.
#include <stdio.h>
#include <conio.h>
void main()
{
    int r = 5;
    float cf;
    system("cls");
    printf("Radius of the Circle = %d", r);
    cf = (float)2 * 22 / 7 * r;
    printf("\nCircumference of the Circle = %f", cf);
}